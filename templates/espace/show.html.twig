{% extends 'headeradmin.html.twig' %}

{% block title %}L'Espace{% endblock %}

{% block content %}
    <h1>Espace</h1>

    <table  = border="2" class="table" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espace.id }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ espace.capacite }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ espace.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ espace.adresse }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ espace.etat }}</td>
            </tr>
             <tr>
                <th>Type Espace</th>
                <td>{{ espace.idType.typeespace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espace_index') }}">retour à la liste</a><br> 

    <a href="{{ path('app_espace_edit', {'id': espace.id}) }}">Modifier</a>

    {{ include('espace/_delete_form.html.twig') }}
{% endblock %}
