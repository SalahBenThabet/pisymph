<form method="post" action="{{ path('app_typespace_delete', {'id': typespace.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet type d' espace?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typespace.id) }}">
    <button class="btn">Delete</button>
</form>
